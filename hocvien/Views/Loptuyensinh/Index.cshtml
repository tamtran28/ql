@model IEnumerable<hocvien.Model.CLopTS>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@if (!string.IsNullOrEmpty(ViewBag.SuccessMessage))
{
    <div id="msgAlert" class="alert alert-success" role="alert">
        @ViewBag.SuccessMessage
    </div>
}

@if (!string.IsNullOrEmpty(ViewBag.SuaSuccessMessage))
{
    <div id="msgAlertS" class="alert alert-success" role="alert">
        @ViewBag.SuaSuccessMessage
    </div>
}
<h1>Danh sách lớp tuyển sinh</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Thêm lớp tuyển sinh</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Mã lớp tuyển sinh
            </th>
            <th>
                Tên lớp tuyển sinh
            </th>
            @*<th>
                    @Html.DisplayNameFor(model => model.Trangthai)
                </th>*@
            @*<th>
                    @Html.DisplayNameFor(model => model.Nguoitao)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Ngaytao)
                </th>*@
            <th>
                Ngày bắt đầu
            </th>
            <th>
                Ngày kết thúc
            </th>
            <th>
                Thứ học
            </th>
            <th>
                Giờ học
            </th>
            @* <th>
                    @Html.DisplayNameFor(model => model.Makh)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Mamh)
                </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Maloptuyensinh)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tenloptuyensinh)
                </td>
                @* <td>
                        @Html.DisplayFor(modelItem => item.Trangthai)
                    </td>*@
                @*  <td>
                        @Html.DisplayFor(modelItem => item.Nguoitao)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Ngaytao)
                    </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.Ngaybatdau.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ngayketthuc.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Thuhoc)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Giohoc)
                </td>
                @* <td>
                        @Html.DisplayFor(modelItem => item.Makh)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Mamh)
                    </td>*@
                <td>
                <td>
                    <a asp-action="formTaolophoc" asp-controller="Xeplop" asp-route-id="@item.Maloptuyensinh" class="btn btn-primary">Tạo lớp học</a>
                </td>
                </td>
            </tr>
        }
    </tbody>
</table>
@if (!string.IsNullOrEmpty(ViewBag.SuccessMessage))
{
    <script>window.onload = function () {
            var alertElement = document.getElementById('msgAlert');
            alertElement.style.display = 'block';
            setTimeout(function () {
                alertElement.style.opacity = '0';
                setTimeout(function () {
                    alertElement.style.display = 'none';
                }, 500); // Thời gian ẩn sau khi mờ dần (đơn vị: milliseconds)
            }, 2000); // Thời gian hiển thị (đơn vị: milliseconds)
        }</script>
}
@if (!string.IsNullOrEmpty(ViewBag.SuaSuccessMessage))
{
    <script>window.onload = function () {
            var alertElement = document.getElementById('msgAlert');
            alertElement.style.display = 'block';
            setTimeout(function () {
                alertElement.style.opacity = '0';
                setTimeout(function () {
                    alertElement.style.display = 'none';
                }, 500); // Thời gian ẩn sau khi mờ dần (đơn vị: milliseconds)
            }, 2000); // Thời gian hiển thị (đơn vị: milliseconds)
        }</script>
}
